# CyberPulse SOC Suite: Dependencies
# Core standard library is prioritized; external packages are kept minimal for clean portability.

# Sigma YAML rule parsing and validation
pyyaml>=6.0.1

# HTTP and Webhook notification dispatch
requests>=2.31.0

# Optional Testing & Linting Development Tools
pytest>=8.0.0
flake8>=7.0.0
